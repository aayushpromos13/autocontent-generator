import { CalendarDays, ChevronLeft, ChevronRight, Info } from 'lucide-react';

export function CalendarPage() {
  const days = Array.from({ length: 35 }, (_, i) => i - 2);
  return <div className="animate-fade-in space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h2 className="text-2xl font-bold tracking-tight text-ink-900">Content calendar</h2><p className="mt-1 text-sm text-ink-500">Plan and schedule your publishing pipeline.</p></div><button className="btn-secondary"><CalendarDays size={17} /> Today</button></div><div className="card overflow-hidden"><div className="flex items-center justify-between border-b border-ink-100 px-5 py-4"><div><h3 className="font-semibold text-ink-900">August 2026</h3><p className="text-xs text-ink-400">No scheduled content yet</p></div><div className="flex gap-1"><button className="btn-ghost p-2"><ChevronLeft size={18} /></button><button className="btn-ghost p-2"><ChevronRight size={18} /></button></div></div><div className="grid grid-cols-7 border-b border-ink-100">{['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].map((day) => <div key={day} className="px-2 py-3 text-center text-[11px] font-semibold uppercase tracking-wide text-ink-400">{day}</div>)}</div><div className="grid grid-cols-7">{days.map((day, i) => { const date = day > 0 && day <= 31 ? day : ''; return <div key={i} className={`min-h-24 border-b border-r border-ink-100 p-2 sm:min-h-28 ${!date ? 'bg-ink-50/50' : 'bg-white'}`}><span className={`text-xs ${day === 20 ? 'flex h-6 w-6 items-center justify-center rounded-full bg-brand-600 font-semibold text-white' : 'text-ink-500'}`}>{date}</span></div>})}</div></div><div className="card flex items-start gap-3 p-5"><Info size={18} className="mt-0.5 shrink-0 text-brand-600" /><div><p className="text-sm font-medium text-ink-800">Your calendar is ready for scheduling</p><p className="mt-1 text-sm text-ink-500">Once you have a video marked as ready, you can schedule it for publishing across your connected channels.</p></div></div></div>;
}
