import { useState } from 'react';
import { Bot, Check, Clock3, Info, Settings2 } from 'lucide-react';
import { DURATIONS, LANGUAGES, NICHES, TONES } from '@/types/database';

export function AutomationPage() {
  const [enabled, setEnabled] = useState(false); const [niche, setNiche] = useState('Technology'); const [frequency, setFrequency] = useState('daily');
  return <div className="mx-auto max-w-4xl animate-fade-in space-y-6"><div><h2 className="text-2xl font-bold tracking-tight text-ink-900">Automation</h2><p className="mt-1 text-sm text-ink-500">Configure a repeatable content engine for your channel.</p></div><section className={`rounded-2xl p-6 text-white shadow-sm sm:p-8 ${enabled ? 'bg-emerald-700' : 'bg-ink-900'}`}><div className="flex flex-col justify-between gap-6 sm:flex-row sm:items-center"><div className="flex items-start gap-4"><div className="flex h-12 w-12 items-center justify-center rounded-xl bg-white/10"><Bot size={25} /></div><div><h3 className="text-lg font-semibold">Daily content generation</h3><p className="mt-1 max-w-md text-sm text-white/70">Automatically create new video projects from your settings on a recurring schedule.</p></div></div><button onClick={() => setEnabled(!enabled)} className={`relative h-7 w-12 shrink-0 rounded-full transition-colors ${enabled ? 'bg-white' : 'bg-white/20'}`} aria-label="Toggle automation"><span className={`absolute top-1 h-5 w-5 rounded-full transition-transform ${enabled ? 'translate-x-6 bg-emerald-600' : 'translate-x-1 bg-white'}`} /></button></div><div className="mt-7 flex items-center gap-2 border-t border-white/10 pt-5 text-sm text-white/70"><Clock3 size={16} />{enabled ? 'Next run will be scheduled after saving.' : 'Automation is currently paused.'}</div></section><section className="card p-6 sm:p-8"><div className="mb-6 flex items-center gap-3"><Settings2 size={19} className="text-brand-600" /><div><h3 className="font-semibold text-ink-900">Generation settings</h3><p className="text-sm text-ink-400">These preferences will guide each automated project.</p></div></div><div className="grid gap-5 sm:grid-cols-2"><div><label className="mb-2 block text-sm font-medium text-ink-700">Niche</label><select className="input" value={niche} onChange={(e) => setNiche(e.target.value)}>{NICHES.map((item) => <option key={item}>{item}</option>)}</select></div><div><label className="mb-2 block text-sm font-medium text-ink-700">Frequency</label><select className="input" value={frequency} onChange={(e) => setFrequency(e.target.value)}><option value="daily">Every day</option><option value="weekdays">Weekdays</option><option value="weekly">Once a week</option></select></div><div><label className="mb-2 block text-sm font-medium text-ink-700">Time of day</label><input type="time" defaultValue="09:00" className="input" /></div><div><label className="mb-2 block text-sm font-medium text-ink-700">Videos per run</label><select className="input"><option>1 video</option><option>2 videos</option><option>3 videos</option></select></div></div><div className="mt-6 grid gap-5 border-t border-ink-100 pt-6 sm:grid-cols-3"><div><label className="mb-2 block text-sm font-medium text-ink-700">Duration</label><select className="input">{DURATIONS.map((item) => <option key={item.seconds}>{item.label}</option>)}</select></div><div><label className="mb-2 block text-sm font-medium text-ink-700">Tone</label><select className="input">{TONES.slice(0, 5).map((item) => <option key={item}>{item}</option>)}</select></div><div><label className="mb-2 block text-sm font-medium text-ink-700">Language</label><select className="input">{LANGUAGES.slice(0, 4).map((item) => <option key={item.code}>{item.label}</option>)}</select></div></div><div className="mt-6 flex items-start gap-3 rounded-lg bg-brand-50 p-4 text-sm text-brand-800"><Info size={17} className="mt-0.5 shrink-0" /><p>Automation settings are saved to your account, but generation will only begin after an AI provider is connected.</p></div><button className="btn-primary mt-6"><Check size={16} /> Save automation settings</button></section></div>;
}
