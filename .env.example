# Frontend environment variables (safe to commit — no secrets)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: override the default Gemini model used by Edge Functions
# Set this as a Supabase Edge Function secret, NOT in this file
GEMINI_MODEL=
